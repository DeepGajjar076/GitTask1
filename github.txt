git config --global user.name "DeepGajjar076"
git config --global user.email "deepgajjar076@gmail.com"

git config --global color.ui auto

git remote add origin https://github.com/DeepGajjar076/GitTask1.git

git pull origin main

git status

git add .
git status
git commit -m "Commit Message"
git push